version: '3'
services: 
    db:
        image: mysql
        ports: 
            - 3306:3306
        environment: 
            MYSQL_ROOT_PASSWORD: password
            MYSQL_DATABASE: noteit
            MYSQL_USER: noteit
            MYSQL_PASSWORD: noteit
    web:
        build: .
        depends_on:
            - db
        environment: 
            HOSTNAME: db
        ports:
        - 8080:8080